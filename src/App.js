import { useState } from "react";

function App() {
  const [notes, setNotes] = useState(null)

  const fetchNotes = () => {
    //Fetch the notes
    // Set to state

  };


  return <div className="App">sdf</div>;
}

export default App;
